apiVersion: apps/v1
kind: Deployment
metadata:
  name: arbor-api
  namespace: wc-apps-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: arbor-api
  template:
    metadata:
      labels:
        app: arbor-api
    spec:
      containers:
        - name: arbor-api
          image: <sonatype-registry>/arbor-api:latest
          ports:
            - containerPort: 3001
          env:
            - name: NODE_ENV
              value: production
            - name: API_PORT
              value: "3001"
          envFrom:
            - secretRef:
                name: arbor-api-secrets
          livenessProbe:
            httpGet:
              path: /healthz
              port: 3001
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /healthz
              port: 3001
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: arbor-api
  namespace: wc-apps-dev
spec:
  selector:
    app: arbor-api
  ports:
    - port: 3001
      targetPort: 3001
  # No Route — only reachable from within the cluster
