{
  "name": "arbor-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "setup": "scripts/setup.sh",
    "build": "turbo build",
    "dev": "dotenv -e apps/bff/.env.local -e apps/api/.env.local -e .env.mock -- turbo dev",
    "dev:mock": "dotenv -e apps/bff/.env.local -e apps/api/.env.local -e .env.mock -- turbo dev",
    "dev:bff": "dotenv -e apps/bff/.env.local -e apps/api/.env.local -e .env.bff  -- turbo dev",
    "dev:oidc": "dotenv -e apps/bff/.env.local -e apps/api/.env.local -e .env.oidc -- turbo dev",
    "dev:staging": "dotenv -e apps/bff/.env.staging.local -e apps/api/.env.local -e .env.staging -- turbo dev",
    "test": "turbo test",
    "typecheck": "turbo typecheck",
    "lint": "eslint framework/*/src apps/*/src",
    "lint:fix": "eslint framework/*/src apps/*/src --fix",
    "format": "prettier --write .",
    "clean": "turbo clean && find framework apps -name 'dist' -type d -prune -exec rm -rf {} +",
    "setup:certs": "bash scripts/setup-certs.sh",
    "db:up": "scripts/db.sh up",
    "db:down": "scripts/db.sh down",
    "db:reset": "scripts/db.sh reset && dotenv -e apps/api/.env.local -- turbo db:migrate",
    "db:migrate": "dotenv -e apps/api/.env.local -- turbo db:migrate",
    "db:generate": "dotenv -e apps/api/.env.local -- turbo db:generate",
    "db:status": "scripts/db.sh status",
    "arbor": "./arbor",
    "plan": "./arbor tui"
  },
  "packageManager": "pnpm@11.3.0",
  "devDependencies": {
    "dotenv-cli": "^11.0.0",
    "eslint": "^9.0.0",
    "prettier": "^3.3.0",
    "prettier-plugin-organize-imports": "^4.0.0",
    "turbo": "^2.0.0",
    "typescript": "catalog:",
    "typescript-eslint": "^8.0.0",
    "vitest": "catalog:"
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=9"
  },
  "peerDependencyRules": {
    "allowedVersions": {
      "nunjucks>chokidar": "4"
    }
  }
}
