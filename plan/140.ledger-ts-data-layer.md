# Plan 140 — TypeScript Ledger Data Layer

## Context

The Python `arbor-cli` package (`tools/arbor-cli/src/arbor_cli/ledger.py`) owns all ledger
types (Pydantic models) and all file I/O (parse JSONL, compute display groups, mutate tasks).
This plan ports that logic to TypeScript, living entirely inside `packages/api/src/ledger/`.
No external routing or React concerns — just data types and pure file operations.

## Goal

A self-contained TypeScript module at `packages/api/src/ledger/` that can:
1. Parse `plan/ledger.jsonl` into a typed in-memory tree.
2. Compute display groups (in-progress, ready, blocked, done, canceled).
3. Mutate a task's fields (status, rank) atomically in the file.

## Change Surface & T-shirt Sizing

**Size: S** (half-day)

New files:
- `packages/api/src/ledger/schemas.ts` — Zod schemas matching Python models
- `packages/api/src/ledger/reader.ts` — parse JSONL, build tree, compute groups
- `packages/api/src/ledger/writer.ts` — update_task: rewrite one task's fields in-place
- `packages/api/src/ledger/index.ts` — barrel re-export

No existing files modified.

## Dependencies

- `zod` (already a dep of `@arbor/api`)
- `node:fs`, `node:path`, `node:url` (built-ins)

## Implementation Notes

### schemas.ts

Port each Pydantic model to a Zod discriminated union:

```typescript
import { z } from 'zod';

export const TaskStatus = z.enum(['in_progress', 'next', 'todo', 'done', 'canceled']);
export type TaskStatus = z.infer<typeof TaskStatus>;

export const Size = z.enum(['xs', 's', 'm', 'l', 'xl']);

export const MetaEntry = z.object({ type: z.literal('meta'), version: z.string(), description: z.string() });
export const EpicEntry = z.object({ type: z.literal('epic'), id: z.string(), title: z.string() });
export const StoryEntry = z.object({ type: z.literal('story'), id: z.string(), epic: z.string(), layer: z.string(), title: z.string() });
export const WaveEntry = z.object({ type: z.literal('wave'), id: z.string(), name: z.string() });
export const TaskEntry = z.object({
  type: z.literal('task'),
  kind: z.enum(['spike', 'task']),
  id: z.number(),
  epic: z.string(),
  story: z.string(),
  wave: z.string(),
  layer: z.string(),
  status: TaskStatus,
  text: z.string(),
  file: z.string(),
  size: Size.optional(),
  deps: z.array(z.number()).default([]),
  rank: z.number().optional(),
});

export type TaskEntry = z.infer<typeof TaskEntry>;
export type EpicEntry = z.infer<typeof EpicEntry>;
export type StoryEntry = z.infer<typeof StoryEntry>;
export type WaveEntry = z.infer<typeof WaveEntry>;

export const LedgerRow = z.discriminatedUnion('type', [MetaEntry, EpicEntry, StoryEntry, WaveEntry, TaskEntry]);
```

### reader.ts

Port `build_hierarchical_ledger` and `compute_display_groups`:

```typescript
import { readFileSync } from 'node:fs';
import { LedgerRow, type TaskEntry, type WaveEntry } from './schemas.js';

export interface DisplayGroups {
  inProgress: TaskEntry[];
  ready: TaskEntry[];
  blocked: { task: TaskEntry; pendingDeps: number[] }[];
  done: TaskEntry[];
  canceled: TaskEntry[];
}

export function parseLedger(path: string): { tasks: TaskEntry[]; waves: WaveEntry[] } { ... }
export function computeDisplayGroups(tasks: TaskEntry[], waves: WaveEntry[]): DisplayGroups { ... }
```

Sort key: `(waveIndex, rank ?? id * 100)` — identical to Python.

### writer.ts

Port `update_task`:

```typescript
export function updateTask(path: string, id: number, updates: Partial<Pick<TaskEntry, 'status' | 'rank'>>): void {
  // Read all lines, find the task line by id, merge updates, write back
}
```

### Ledger path resolution

```typescript
// ledger/path.ts
import { fileURLToPath } from 'node:url';
import { join, dirname } from 'node:path';

export function ledgerPath(): string {
  return process.env['ARBOR_LEDGER_PATH'] ?? join(repoRoot(), 'plan', 'ledger.jsonl');
}

function repoRoot(): string {
  // Walk up from __dirname until we find plan/ledger.jsonl
  let dir = dirname(fileURLToPath(import.meta.url));
  while (dir !== '/') {
    if (existsSync(join(dir, 'plan', 'ledger.jsonl'))) return dir;
    dir = dirname(dir);
  }
  throw new Error('Could not locate plan/ledger.jsonl — set ARBOR_LEDGER_PATH');
}
```

## Success Criteria

- `parseLedger(ledgerPath())` returns tasks + waves without errors when run against the real file.
- `computeDisplayGroups` produces the same groupings as the Python version for a fixed fixture.
- `updateTask` rewrites a task's status and leaves all other lines unchanged (test with a temp file copy).
- No TypeScript errors under `pnpm --filter @arbor/api typecheck`.
