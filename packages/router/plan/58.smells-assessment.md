# Plan 58 — Architectural Smell Assessment

## Context

This document is a holistic review of the router package to surface long functions,
structural inconsistencies, ergonomic traps, and naming confusion. It serves as the
master backlog for smell-driven cleanup work and cross-references existing and new plans.

Completed context: Plans 56 (core/context decoupling) and 57 (Enricher → Guard rename)
addressed two of the highest-priority structural issues. This document captures what remains.

---

## Smell Inventory

### HIGH — fix before growing the API surface

| ID | Smell | Location | Plan |
|----|-------|----------|------|
| H1 | `createServer()` is a 221-line god function handling 8 distinct concerns: method validation, rate limiting, handler resolution, body/header/cookie validation, execution, response validation, and error handling. Individual concerns are untestable in isolation. | `server/server.ts` | Plan 60 ✅ |
| H2 | `walkParse()` is a 92-line function mixing segment matching, param accumulation, query schema resolution, Zod validation (twice), child recursion, and diagnostic collection. Six concerns in one function. | `core/walk.ts` | Plan 59 ✅ |
| H3 | `Enricher` is a misnomer for what is actually a guard/short-circuit; its discriminated union `{ ok: true; ctx } \| { ok: false; response }` duplicates `Result`'s `{ ok: true; value } \| { ok: false; error }` with different field names, creating two parallel abstractions that can't interop. `composeEnrichers` is 2-arity only. | `server/enrichers.ts` | Plan 57 ✅ |

### MEDIUM — structural correctness issues

| ID | Smell | Location | Plan |
|----|-------|----------|------|
| M1 | `_meta` is `Record<string, unknown>` everywhere; each consumer casts it to a different local shape (`HttpContextData`, `OpenApiCtxData`, `{ querySchema?: ZodObject }`). No central type contract. Easy to add a field in one place and forget it in another. | `core/route-node.ts`, all context files | Plan 61 ✅ |
| M2 | Three parallel response shapes serve overlapping roles: `DispatchResult` in `server.ts`, `HttpResponseUnion` in `http-context.ts`, and raw handler return objects. No single canonical `HttpResponse` type. | `server/server.ts`, `contexts/http-context.ts` | Plan 62 ✅ |
| M3 | `httpRoute()` descriptor unpacking uses fragile duck-typing: `'safeParse' in d` to detect a ZodType, `'body' in d` to detect a descriptor object. No type guard or discriminant. Silently misbehaves if descriptor is an object with no `body` key. | `contexts/http-context.ts:131-156` | Plan 62 ✅ |
| M4 | `walkParse`, `walkCollect`, `walkPrint` each repeat the same pattern: null-schema check, tag extraction, `Object.assign` recursion — DRY violation across three functions in one file. `buildMethodMap` and `buildResponseSchemaMap` in `fetch-client.ts` are two more near-identical walkers. | `core/walk.ts`, `client/fetch-client.ts` | Plan 59 ✅ |
| M5 | Unsafe type casts throughout: `router.children as WalkNode[]`, `handlers as Record<string, UntypedHandler>`, `route as Record<string, unknown>`. All hide real types and make refactoring invisible to the compiler. | `server/server.ts`, `core/walk.ts` | Plan 60 ✅ |
| M6 | `walkSpec()` in `generate-spec.ts` is 112 lines covering 6 concerns (wildcard filtering, path params, query params, header params, operation metadata, request/response bodies). `isRequired()` pattern is duplicated inline at lines 70 and 82. `generateSpec` is also re-exported from `openapi-context.ts`, creating a spurious second public path. | `openapi/generate-spec.ts`, `contexts/openapi-context.ts` | Plan 63 |
| M7 | `openApiRoute()` constructs a node via `httpRoute()`, then casts and mutates `node._meta` to add OpenAPI-specific fields: `(node._meta as OpenApiCtxData).meta = options.meta`. Mutation through a cast; breaks if `httpRoute` returns a sealed object. The return type is also force-cast. | `contexts/openapi-context.ts:57-61` | Plan 64 |
| M8 | Rate limiting is hard-coded as the first step inside `executeRoute()`, mixing policy enforcement (with side-effects and external state) with stateless schema validation. Untestable in isolation; can't be moved to a different integration point without touching the function. | `server/server.ts:142-148` | Plan 66 |

### LOW-MEDIUM — ergonomic friction for API users

| ID | Smell | Location | Plan |
|----|-------|----------|------|
| E1 | `status: 200 as const` required on every handler return for inference to work. Appears 30+ times across tests and examples. | All handler definitions | Plan 62 ✅ |
| E2 | Two response definition syntaxes: bare `{ 200: Schema }` vs descriptor `{ 200: { body: Schema, headers: H } }`. Same-looking, different behavior, no immediate error when you use the wrong form. | `http-context.ts:74-89`, examples | Plan 62 ✅ |
| E3 | Every enricher/guard requires explicit context threading in its type: `Enricher<BaseCtx & PriorExtra, NewExtra>`. No inference chain — authors must manually track accumulated context. | `server/enrichers.ts`, `examples/enrichers.ts` | Plan 57 ✅ |
| E4 | Query params are split across two places in the client: path/matrix params go on the route tag object, query params go in the options bag. Inconsistent data flow model. | `client/fetch-client.ts`, `examples/query-params.ts` | Plan 48 area |
| E5 | `TypedClient` requires manual type extraction at the call site: `TypedClient<Router['_type'], Router['_ctxMap']>`. No shorthand. | `examples/typed-client.ts:50-54` | Plan 48 area |
| E6 | `ParseDiag` and `HandlerCtx` are not exported from the package root `src/index.ts`, forcing users to import from internal paths (`core/walk.js`, `server/server.js`) that break after publish. | `src/index.ts` | Plan 65 |

### LOW — clean-up / future-proofing

| ID | Smell | Location | Plan |
|----|-------|----------|------|
| L1 | Single-letter type params (`S`, `C`, `R`) carry non-obvious semantics. | `core/route-node.ts`, `contexts/http-context.ts` | cosmetic |
| L2 | OpenAPI segment-to-param mapping uses two parallel `switch`/`case` functions with no shared structure. Hard to extend for new segment kinds. | `openapi/generate-spec.ts:14-33` | low priority |
| L3 | `FlattenChildrenImpl` depth-counter (15-level tuple-counting to avoid TS2589) is a clever but brittle workaround. Magic number buried in the type system. | `core/route-node.ts` | Plan 47 ✅ (no regression yet) |
| L4 | `src/contexts/index.ts` and `src/security/index.ts` are empty (0 bytes) — scaffolding artifacts that export nothing and are not imported by anything. | `src/contexts/index.ts`, `src/security/index.ts` | Plan 65 |

### Segment system gaps

| ID | Smell | Location | Plan |
|----|-------|----------|------|
| S1 | `num`/`opt-num` use `Number()` + `isNaN` which accepts decimals (`1.5`) and scientific notation (`1e3`). OpenAPI emits `integer` but runtime matches any finite number. | `core/segments.ts:57-62, 71-79` | Plan 67 |
| S2 | Wildcard captures `string[]` not `string`. Every caller must `.join('/')` manually. `buildUrl` does not special-case wildcard so `print()` produces comma-joined garbage for wildcard params. | `core/segments.ts:83-85`, `core/walk.ts buildUrl` | Plan 68 |
| S3 | Optional segments (`opt-str`, `opt-num`) are greedy and silently produce wrong matches when placed before a required segment in the same path string. A section with `':lang?'` as prefix cannot route to required-segment children. Documented in test comments but not enforced. | `core/segments.ts`, `core/segments.test.ts:225` | Plan 69 |
| S4 | No routing-level pattern/format constraint on string params. Format validation deferred entirely to Zod, which means a format mismatch produces a parse error rather than trying the next sibling route. | `core/segments.ts` | Plan 70 (LOW) |

---

## Recommended Execution Order

| Wave | Plans | Status | Rationale |
|------|-------|--------|-----------|
| 1 | Plan 57 | ✅ Done | Rename only — zero risk, unblocks clear thinking about guards |
| 2 | Plan 59 | ✅ Done | Walk decomposition — foundational; server and client both depend on walk |
| 3 | Plan 61 | ✅ Done | `_meta` typing — eliminates cast pattern before 62 lands |
| 4 | Plan 60 | ✅ Done | Server decomposition — depends on walk being clean |
| 5 | Plan 62 | ✅ Done | Response unification — depends on server structure being settled |
| 6 | Plan 65 | Open | Barrel cleanup + export gaps — zero risk, standalone |
| 7 | Plan 67 | Open | `num` integer-only — tiny, isolated, no dependencies |
| 8 | Plan 69 | Open | Optional segment ordering enforcement — correctness, `segments.ts` only |
| 9 | Plan 68 | Open | Wildcard as string — breaking change, do after 69 stabilises segments |
| 10 | Plan 63 | Open | `walkSpec()` decomposition + spurious re-export fix |
| 11 | Plan 64 | Open | `openApiRoute()` cast-and-mutate fix — isolated to one file |
| 12 | Plan 66 | Open | Rate limit decoupling — internal refactor, no public API change |
| — | Plan 70 | Open (LOW) | Pattern segment — enhancement, after segment correctness is settled |

E4 and E5 are deferred to the Plan 48 area (typed HTTP client redesign).
L1 and L2 are cosmetic; address opportunistically during other plans.

---

## Goal

This document is a reference backlog, not an executable plan. Each referenced plan number
is the unit of work to execute.

## Change Surface

No code changes in this plan — it is documentation only.

## Dependencies

Plans 59–62 depend on Plan 57 completing first (cleaner guard semantics aid reasoning
about server decomposition).

## Success Criteria

- Plans 57, 59, 60, 61, 62 all land with green verification chains.
- No `as any` or unexplained casts remain in `server.ts` or `walk.ts`.
- The `_meta` cast pattern (`node._meta as HttpContextData`) is eliminated.
- `status: N as const` boilerplate is eliminated from all handler definitions.
- `pnpm test && pnpm typecheck && pnpm lint && pnpm run examples` clean.
